@model IEnumerable<Prueba2.Cliente>

@{
    ViewBag.Title = "Index";
}

<h2>Clientes</h2>

<p>
    @Html.ActionLink("Crear nuevo cliente", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Nombre
        </th>
        <th>
           Apellidos
        </th>
        <th>
            Nombre empleado
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.apellidos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empleado.nombre)
        </td>
        <td>
            @Html.ActionLink(" ", "Edit", new { id=item.id_cliente }, new { @class = "btn btn-info glyphicon glyphicon-edit" }) |
            @Html.ActionLink(" ", "Details", new { id=item.id_cliente }, new { @class = "btn btn-success glyphicon glyphicon-info-sign" }) |
            @Html.ActionLink(" ", "Delete", new { id=item.id_cliente }, new { @class = "btn btn-danger glyphicon glyphicon-remove" })
        </td>
    </tr>
}

</table>
